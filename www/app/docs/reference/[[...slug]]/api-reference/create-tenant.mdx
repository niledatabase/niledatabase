export const meta = {
  title: "Create tenant",
  order: 3,
  method: "POST",
};

# Create a tenant

This is placeholder, for when we actually document this thing. hurray!

<Row>
  <Col>
Usage

Something is happening here, and it's real good

Parameters

  </Col>
  <Col>
    <CodeGroups
      title="Create tenant"
      method="POST"
      pathname="/workspace/:workspace/database/:database/create-tenant"
    >
    ```typescript
import ApiClient from '@example/protocol-api'
const client = new ApiClient(token)
await client.conversations.list()
    ```

    ```bash
    curl thenile.dev/workspace/<my workspace>/
    ```

</CodeGroups>

  </Col>

</Row>
```
