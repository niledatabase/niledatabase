export const metadata = {
  title: "Update tenant",
  order: 4,
  method: "PUT",
};

# Update a Tenant

    <div className="grid grid-cols-2 gap-4">
    You can rename tenants with a PUT request.
    <CodeGroups
      title={"Update Tenant Request"}
      method={"PUT"}
      pathname={"/v2/databases/:databaseId/tenants/:tenantId"}
    >
      ```bash
      curl 'https://eu-central-1.api.thenile.dev/v2/databases/<string>/tenants/<string>' \
      --header 'Authorization: Bearer <string>' \
      --data '{
        "name": "<string>"
      }'
      ```
    </CodeGroups>
    The response includes the generated tenant id and the name you provided.
    <CodeGroups
      title={"Update Tenant Response"}
      response={"200"}
      pathname={"/v2/databases/:databaseId/tenants/:tenantId"}
    >
      ```json
      {
        "id": "018b61ec-af9e-77e5-b27f-77239cd4a0bc",
        "name": "VIP Customer"
      }
      ```
    </CodeGroups>

</div>
