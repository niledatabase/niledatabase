export const metadata = {
  title: "List credentials",
  order: 4,
  method: "GET",
};

# List Database Credentials

<Row>
  <Col>
    List all of the credentials created for a database.
  </Col>
  <Col>
    <CodeGroups
      title={"List Credentials Request"}
      method={"GET"}
      pathname={"/workspaces/:workspaceSlug/databases/:databaseName/credentials"}
    >
      ```bash
      curl 'https://api.thenile.dev/workspaces/<string>/databases/<string>/credentials' \
      --header 'Authorization: Bearer <string>'
      ```
    </CodeGroups>
  </Col>
</Row>
<Row>
  <Col>
    The response includes the id of each credential—not the password. Use these ids to keep track of your credentials.
  </Col>
  <Col>
    <CodeGroups
      title={"List Credentials Response"}
      method={"200"}
      pathname={"/workspaces/:workspaceSlug/databases/:databaseName/credentials"}
    >
      ```json
      [
        {
          "id": "018b61de-179d-7ddb-a64b-d0ae0a2bafa9",
          "database": {
            "id": "018b61bc-cf2c-7392-92a6-f1a811cf4419",
            "name": "database_one",
            "workspace": {
              "name": "My Workspace",
              "slug": "my-workspace"
            }
          }
        }
      ]
      ```
    </CodeGroups>
  </Col>
</Row>
