export const metadata = {
  title: "List workspaces",
  order: 3,
  method: "GET",
};

# List Workspaces

<div className="grid grid-cols-2 gap-4">
    List all of the workspaces that you have access to.

    <CodeGroups
      title={"List Workspace Request"}
      method={"GET"}
      pathname={"/workspaces"}
    >
      ```bash
      curl 'https://api.thenile.dev/workspaces' \
      --header 'Authorization: Bearer <string>'
      ```
    </CodeGroups>

    The response includes the name and slug of every workspace you are authorized to access.
    <CodeGroups
      title={"List Workspace Response"}
      response={"200"}
      pathname={"/workspaces"}
    >
      ```json
      [
        {
          "name": "My First Workspace",
          "slug": "my-first-workspace"
        },
        {
          "name": "My Second Workspace",
          "slug": "my-second-workspace"
        }
      ]
      ```
    </CodeGroups>

</div>
